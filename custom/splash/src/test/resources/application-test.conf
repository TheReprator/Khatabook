ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ dev.reprator.splash.ApplicationKt.module ]
    }

    database {
        driverClass = "org.h2.Driver"
        url = "jdbc:h2:mem:db"
        user = "root"
        password = "password"
        maxPoolSize = 10
    }
}


khatabook {
    splash {
        dir = splashFileDirectory
    }
}
